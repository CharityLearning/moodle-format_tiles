pipelines:
    branches:
      master:
        - step:
           script:
             - git remote add sync git@github.com:CharityLearning/moodle-format_tiles.git
             - git checkout master
             - git pull    
             - git push sync master
      moodle40:
        - step:
           script:
             - git remote add sync git@github.com:CharityLearning/moodle-format_tiles.git
             - git checkout moodle40
             - git pull    
             - git push sync moodle40